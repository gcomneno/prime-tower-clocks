# PTC Classic (v1)

Questa è la prima incarnazione di Prime Tower Clocks:
- firma come lista di clock primi `{p,z,e}` in base 2
- export JSONL per integrazione
- SSOT binario PTCBIN
- ricostruzione via CRT con condizione `M > N` per lossless

Motivazione/visione: rappresentazione modulare progressiva ("microscopio") nata come intuizione notturna.
